var _ = require('underscore'),
    Class = require('sji');

var Validation = module.exports = Class.extend({
    init:function () {
    },
    is_valid:function (json, callback) {
        callback(null, {});
    }
});